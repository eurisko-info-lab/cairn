-- Ported from Marble/Charb `federation-registry-workflow.yaml`
-- Source-file: federation-registry-workflow.yaml
-- Description: Federation trust registry listing, inspection, and status summary
-- Porcelain/plumbing promotion (docs/porcelain.md); not a Marble CLI clone.
transcript federation-registry {
  porcelain federation-registry ;
}
