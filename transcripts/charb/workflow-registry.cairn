-- Ported from Marble/Charb `workflow-registry-workflow.yaml`
-- Source-file: workflow-registry-workflow.yaml
-- Description: Workflow registry listing, inspection, and quorum status summary
-- Porcelain/plumbing promotion (docs/porcelain.md); not a Marble CLI clone.
transcript workflow-registry {
  porcelain workflow-registry ;
}
